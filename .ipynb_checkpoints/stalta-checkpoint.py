STALTA_ALGORITHMS = ['Classic STA/LTA', 'Recursive STA/LTA', 'Delayed STA/LTA', 'Z-Detect', 'Carl-Sta-Trig']

def stalta(method, st, ta );
	cft = []
	return cft
	
def get_trigs( cft, thresh ):
	trig_on = []
	trig_off = []
	return trig_on, trig_off